{
  "name": "photogen",
  "version": "0.0.10",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "author": "Flavian DESVERNE <desverne@prisma.io>",
  "files": [
    "dist",
    "nexus_prisma_ncc_build"
  ],
  "bin": {
    "photogen": "dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && rm -rf nexus_prisma_ncc_build && tsc && ncc build -e nexus-tmp-chainable-method src/nexus-prisma/index.ts -o nexus_prisma_ncc_build",
    "prepublish": "yarn build",
    "prettify": "prettier --write src/**/*.ts"
  },
  "dependencies": {
    "nexus-tmp-chainable-method": "0.0.4"
  },
  "devDependencies": {
    "@prisma/cli": "^0.0.13",
    "@types/node": "^12.0.8",
    "@zeit/ncc": "^0.19.1",
    "prettier": "^1.17.1",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.2"
  },
  "prettier": {
    "semi": true,
    "singleQuote": true
  }
}
